# Semantic Atlas Preview

Public, read-only preview of the Semantic Atlas visualization layer.

This repository contains a privacy-safe static projection only. Canonical/internal data, credentials and backend services are not included.
